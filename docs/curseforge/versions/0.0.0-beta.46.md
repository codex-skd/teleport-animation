<h2>v0.0.0-beta.46 - Fix Frustum prepare with animation camera position</h2>

<h3>Fix</h3>
<ul>
<li><strong>Chunk culling during animation</strong>: Changed <code>FrustumMixin</code> from modifying fields at TAIL to modifying method parameters at HEAD using <code>@ModifyVariable</code>. Now <code>Frustum.prepare</code> receives the animation camera position directly, ensuring both frustum planes and distance culling are calculated from the correct position.</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>teleport_animation-26.1.2-neoforge-0.0.0-beta.46.jar</code></p>
