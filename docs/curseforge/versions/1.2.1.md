<h2>Teleport Animation v1.2.1</h2>
<h3>Fixed</h3>
<ul>
<li><strong>Server-side log spam:</strong> 13 client-only mixins were registered in the common list instead of the client-only list, so a dedicated server logged 13 <code>ERROR</code> + 13 <code>WARN</code> lines on every boot (safely blocked by NeoForge, no functional impact). Moved to the client-only mixin list &ndash; no behaviour change on either client or server.</li>
</ul>

<hr>
