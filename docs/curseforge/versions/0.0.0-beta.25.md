<h1>v0.0.0-beta.25</h1>
<h2>Fix</h2>
<ul>
<li><strong>Waystones mixin method matching</strong> — removed explicit method descriptors from <code>@Inject</code> targets, using only method names with <code>remap=false</code> for <code>@Pseudo</code> mixin matching.</li>
</ul>