<h2>v0.0.0-beta.6 - Fix animation freeze after command send</h2>

<h3>Fix</h3>
<ul>
<li><strong>Animation stuck mid-transition</strong>: <code>++ticks</code> was inside the command send condition, so after the command was sent, <code>ticks</code> stopped incrementing and the animation froze permanently. Moved tick increment to run every frame unconditionally.</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>teleport_animation-1.21.1-neoforge-0.0.0-beta.6.jar</code></p>
