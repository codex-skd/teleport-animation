<h1>v0.0.0-beta.30</h1>
<h2>Fix</h2>
<ul>
<li><strong>Terrain rendering during animation</strong> — <code>ViewArea.repositionCamera()</code> now called via reflection with <code>(double, double)</code> instead of the removed <code>(SectionPos)</code> overload. Prevents chunks from disappearing during the animation.</li>
</ul>