<h1>v0.0.0-beta.30</h1>
<h2>Fix</h2>
<ul>
<li><strong>Terrain rendering during animation</strong> — <code>ViewArea.repositionCamera()</code> ahora se llama por reflexi&oacute;n con <code>(double, double)</code> en lugar del antiguo <code>(SectionPos)</code> que no existe en MC 26.1.2. Esto evita que los chunks desaparezcan durante la animaci&oacute;n.</li>
</ul>