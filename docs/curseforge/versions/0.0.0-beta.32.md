<h1>v0.0.0-beta.32</h1>
<h2>Fix</h2>
<ul>
<li><strong>Diagnostic log location</strong> — totalTicks log was accidentally placed in <code>recordActualTargetFeet</code> instead of <code>start()</code>. Now correctly logs on transition start.</li>
</ul>