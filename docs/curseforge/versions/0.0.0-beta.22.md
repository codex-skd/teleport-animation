<h1>v0.0.0-beta.22</h1>
<h2>Fix</h2>
<ul>
<li><strong>EntityMixin crash on startup</strong> — corrected <code>teleportTo</code> descriptor: MC 26.1.2 now has 8 parameters (includes <code>boolean</code>) and returns <code>boolean</code>. Changed <code>CallbackInfo</code> to <code>CallbackInfoReturnable&lt;Boolean&gt;</code>.</li>
<li><strong>ServerPlayerMixin</strong> — same <code>teleportTo</code> fix applied.</li>
</ul>