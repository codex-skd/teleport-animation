<h1>v0.0.0-beta.24</h1>
<h2>Fix</h2>
<ul>
<li><strong>Waystones integration</strong> — updated for Waystones 26.1.2.10 API changes (<code>WaystoneTeleportManager.tryTeleport</code> instead of removed <code>PlayerWaystoneManager.tryTeleport</code>).
<li><strong>Template location</strong> — moved from <code>src/main/templates/</code> to <code>src/main/resources/templates/</code> per WORKFLOW_GENERIC v1.0.0.</li>
</ul>
<h2>Technical Changes</h2>
<ul>
<li><code>WaystonesTeleportHandler</code> — removed <code>getDestination()</code> reflection calls (API removed), now uses <code>getTargetWaystone()</code> + <code>getPos()</code>/<code>getDimension()</code>.</li>
<li><code>WaystonesPlayerWaystoneManagerMixin</code> — target changed from <code>PlayerWaystoneManager</code> to <code>WaystoneTeleportManager</code>.</li>
<li>Removed <code>nul</code> file, created <code>temp/</code> directory, updated <code>.gitignore</code>.</li>
<li>Updated <code>mod_name</code> from <code>Grand Teleport</code> to <code>Teleport Animation</code>.</li>
<li>Updated <code>WORKFLOW_TELEPORT_ANIMATION_26-1-2.md</code> to v1.0.0.</li>
</ul>