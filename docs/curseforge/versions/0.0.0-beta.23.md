<h1>v0.0.0-beta.23</h1>
<h2>Fix</h2>
<ul>
<li><strong>ServerPlayerMixin crash on startup</strong> — removed <code>teleportTo</code>/<code>changeDimension</code> injects from ServerPlayerMixin (methods are inherited from Entity and already handled by EntityMixin via <code>instanceof ServerPlayer</code> check).</li>
</ul>